@page
@model WebApplication1.Pages.Courses.IndexModel

<p>
    <a asp-page="Create">Создать запись</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Номер курса
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Course) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumberCourse)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.idCourse">Изменить</a> |
                <a asp-page="./Details" asp-route-id="@item.idCourse">Просмотр</a> |
                <a asp-page="./Delete" asp-route-id="@item.idCourse">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>

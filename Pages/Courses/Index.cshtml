@page
@model WebApplication1.Pages.Courses.IndexModel

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Course[0].NumberCourse)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Course) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumberCourse)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.idCourse">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.idCourse">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.idCourse">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

@page
@model WebApplication1.Pages.LiveInAHostels.IndexModel

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LiveInAHostel[0].IdStudent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LiveInAHostel[0].NumberRoom)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.LiveInAHostel) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdStudent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumberRoom)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdEntryLiveInAHostel">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdEntryLiveInAHostel">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdEntryLiveInAHostel">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

@page
@model WebApplication1.Pages.LiveInAHostels.IndexModel

<p>
    <a asp-page="Create">Создать запись</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Студент
            </th>
            <th>
                Номер комнаты
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.LiveInAHostel) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdStudent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumberRoom)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdEntryLiveInAHostel">Изменить</a> |
                <a asp-page="./Details" asp-route-id="@item.IdEntryLiveInAHostel">Просмотр</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdEntryLiveInAHostel">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>

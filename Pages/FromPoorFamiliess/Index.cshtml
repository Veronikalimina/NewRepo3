@page
@model WebApplication1.Pages.FromPoorFamiliess.IndexModel

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FromPoorFamilies[0].IdStudent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.FromPoorFamilies) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdStudent)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdEntryFromPoorFamilies">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdEntryFromPoorFamilies">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdEntryFromPoorFamilies">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

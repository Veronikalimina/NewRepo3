@page
@model WebApplication1.Pages.Curators.IndexModel

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Curator[0].Surname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Curator[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Curator[0].MiddleName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Curator) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Surname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MiddleName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdCurator">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdCurator">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdCurator">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

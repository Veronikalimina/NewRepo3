@page
@model WebApplication1.Pages.CuratorObservationSheets.IndexModel

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CuratorObservationSheet[0].IdStudent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CuratorObservationSheet[0].BriefDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CuratorObservationSheet) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdStudent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BriefDescription)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdCuratorObservationSheet">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdCuratorObservationSheet">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdCuratorObservationSheet">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

@page
@model WebApplication1.Pages.CuratorObservationSheets.IndexModel

<p>
    <a asp-page="Create">Создать запись</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Студент
            </th>
            <th>
                Характеристика
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CuratorObservationSheet) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdStudent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BriefDescription)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdCuratorObservationSheet">Изменить</a> |
                <a asp-page="./Details" asp-route-id="@item.IdCuratorObservationSheet">Просмотр</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdCuratorObservationSheet">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>

@page
@model WebApplication1.Pages.Users.IndexModel

<p>
    <a asp-page="Create">Создать запись</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Тип пользователя
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.User) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdUser">Изменить</a> |
                <a asp-page="./Details" asp-route-id="@item.IdUser">Просмотр</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdUser">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>

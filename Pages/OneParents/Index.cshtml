@page
@model WebApplication1.Pages.OneParents.IndexModel

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OneParent[0].IdStudent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.OneParent) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdStudent)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdEntryOneParent">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdEntryOneParent">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdEntryOneParent">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

@page
@model WebApplication1.Pages.FromLargeFamiliess.EditModel

<h4>FromLargeFamilies</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="FromLargeFamilies.IdEntryFromLargeFamilies" />
            <div class="form-group">
                <label asp-for="FromLargeFamilies.IdStudent" class="control-label">Студент</label>
                <input asp-for="FromLargeFamilies.IdStudent" class="form-control" />
                <span asp-validation-for="FromLargeFamilies.IdStudent" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Сохранить" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Вернуться к таблице</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

@page
@model WebApplication1.Pages.FromLargeFamiliess.IndexModel

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FromLargeFamilies[0].IdStudent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.FromLargeFamilies) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdStudent)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdEntryFromLargeFamilies">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdEntryFromLargeFamilies">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdEntryFromLargeFamilies">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

@page
@model WebApplication1.Pages.Orphanss.IndexModel

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Orphans[0].IdStudent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Orphans) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdStudent)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdEntryOrphans">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdEntryOrphans">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdEntryOrphans">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

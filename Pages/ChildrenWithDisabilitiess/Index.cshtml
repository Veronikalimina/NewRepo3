@page
@model WebApplication1.Pages.ChildrenWithDisabilitiess.IndexModel

<p>
    <a asp-page="Create">Создать запись</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Студент
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ChildrenWithDisabilities) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdStudent)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdEntryChildrenWithDisabilities">Изменить</a> |
                <a asp-page="./Details" asp-route-id="@item.IdEntryChildrenWithDisabilities">Просмотр</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdEntryChildrenWithDisabilities">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>

@page
@model WebApplication1.Pages.ChildrenWithDisabilitiess.IndexModel

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ChildrenWithDisabilities[0].IdStudent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ChildrenWithDisabilities) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdStudent)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdEntryChildrenWithDisabilities">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdEntryChildrenWithDisabilities">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdEntryChildrenWithDisabilities">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
